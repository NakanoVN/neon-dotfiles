#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
alias v='vim'
export EDITOR=nvim
export MANPAGER='nvim +Man!'
alias st-build='cd ~/.config/suckless/st && sudo make install'

PS1='[\u@\h \W]\$ '
